# DeepSeek 访问日志实施计划

1. 升级现有 IndexedDB，并新增访问日志仓库及查询、更新、清理、清空方法。
   - 验证：升级后保留翻译缓存仓库；日志按时间查询；30 天和 1,000 条限制生效。
2. 为 API Key 测试和翻译请求接入统一日志生命周期与错误分类。
   - 验证：成功、配置、HTTP、网络、超时、取消、解析和结构错误均得到预期日志，且日志失败不影响翻译。
3. 在弹窗增加日志概况、JSONL 导出和清空操作。
   - 验证：空状态明确；导出逐行可解析；清空后数量归零；无需新增权限。
4. 更新中英文文案和用户文档。
   - 验证：locale JSON 可解析；三份文档明确说明完整请求正文、失败响应、保留规则和隐私风险。
5. 执行静态检查、模拟场景测试、敏感信息扫描和补丁检查。
   - 验证：JavaScript 语法、JSON、测试桩和 `git diff --check` 全部通过，工作区既有改动未被纳入。
