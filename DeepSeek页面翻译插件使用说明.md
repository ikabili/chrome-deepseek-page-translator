# DeepSeek 页面翻译插件使用说明

## 一、插件简介

DeepSeek 页面翻译插件是一个 Chrome MV3 扩展，用于将网页界面文本翻译为目标语言。插件可以通过 zip 包共享给其他使用者；每位使用者解压后自行加载扩展，并配置自己的 DeepSeek API Key。插件支持按域名开启全站持续翻译，并使用本地 IndexedDB 建立翻译记忆库。相同网站、相同目标语言、相同原文再次出现时，会优先使用本地缓存，不再调用 DeepSeek API。

## 二、核心能力

- 支持 DeepSeek API Key 本地配置
- 支持按当前域名开启或关闭全站持续翻译
- 支持动态网页和 SPA 页面增量翻译
- 支持 IndexedDB 本地翻译记忆库
- 支持 90 天未命中的翻译缓存自动清理
- 支持缓存命中后直接替换文本
- 支持 DeepSeek API 有限并发请求
- 支持未返回翻译时在原文后显示旋转加载图标
- 支持恢复当前页面原文
- 支持清除当前域名缓存
- 支持查看、关闭、删除已开启全站翻译的网站
- 支持 DeepSeek 官方鲸鱼图标

## 三、安装方式

1. 获取插件 zip 包。
2. 将 zip 包解压到本机任意目录，例如桌面、下载目录或自定义工具目录。
3. 打开 Chrome。
4. 访问 `chrome://extensions/`。
5. 开启右上角的「开发者模式」。
6. 点击「加载已解压的扩展程序」。
7. 选择第 2 步得到的实际解压目录，也就是包含 `manifest.json` 的那个文件夹。
8. 加载完成后，Chrome 工具栏会显示 DeepSeek 鲸鱼图标。

注意：Chrome 开发者模式下不能直接加载 zip 文件，必须先解压，再选择解压后的文件夹。不同使用者的目录地址不需要相同，以各自电脑上的实际解压位置为准。

## 四、首次配置

1. 打开任意网页。
2. 点击 Chrome 工具栏里的 DeepSeek 鲸鱼图标。
3. 在 `API Key` 输入框填写自己的 DeepSeek API Key。
4. 选择目标语言。
5. 选择模型：
   - `V4 Flash`：推荐默认使用，速度和成本更适合网页翻译。
   - `V4 Pro`：适合更复杂或更高质量要求的内容。
6. 点击「保存设置」。

API Key 会保存在当前使用者自己的 Chrome 扩展本地配置中，也就是 `chrome.storage.local`。共享插件 zip 包时不会包含任何人的 API Key。每位使用者都需要填写自己的 DeepSeek API Key。

## 五、翻译当前页面

点击「翻译当前页」后，插件会扫描当前页面可见文本，并执行以下流程：

1. 过滤不适合翻译的内容，例如脚本、样式、输入框、代码块、画布等。
2. 将文本节点整理为待翻译列表。
3. 先查询 IndexedDB 本地翻译记忆库。
4. 缓存命中的文本立即替换为译文。
5. 缓存未命中的文本调用 DeepSeek API。
6. API 返回后写回页面，并保存到本地翻译记忆库。

## 六、开启当前域名全站翻译

在插件弹窗右上角打开开关后，当前域名会进入全站持续翻译模式。

例如当前页面是：

```text
example.com
```

开启后，插件会保存：

```json
{
  "sites": {
    "example.com": {
      "enabled": true,
      "targetLang": "zh-CN"
    }
  }
}
```

以后访问 `example.com` 的任意页面，插件会自动翻译页面内容。

## 七、动态内容和 SPA 页面

插件会通过 `MutationObserver` 监听新增 DOM 内容，并通过 URL 变化检测支持 SPA 页面。

适用场景包括：

- 页面滚动后加载新内容
- 前端路由切换
- 弹窗或菜单动态出现
- 列表或评论增量加载

新增内容会进入翻译队列，已经翻译过的文本不会重复翻译。

## 八、本地翻译记忆库

插件使用 Chrome 扩展自己的 IndexedDB 保存翻译缓存。

数据库信息：

```text
数据库名：deepseek-page-translator
对象仓库：translations
```

缓存 key 由以下内容组成：

```text
域名 + 目标语言 + 原文 SHA-256
```

因此，同一个网站、同一个目标语言、同一段原文再次出现时，会直接使用本地译文。

插件会记录每条翻译缓存的最近命中或更新时间。如果某条翻译缓存连续 90 天没有命中，插件会在后续使用过程中自动清理该记录。自动清理每天最多执行一次，避免频繁扫描本地数据库影响翻译速度。

注意：IndexedDB 数据由 Chrome 管理，不能指定保存到插件 workspace 目录，也不会在插件目录生成 `.db` 文件。首次使用时 Chrome 会自动创建数据库。

## 九、API 并发策略

插件不会无限并发调用 DeepSeek API，而是采用有限并发。

当前规则：

```text
每个请求最多 30 条文本
1-59 条未命中：1 并发
60-89 条未命中：2 并发
90 条及以上未命中：3 并发
```

示例：

```text
90 条未命中 -> 30 + 30 + 30，3 个请求并发
120 条未命中 -> 先并发 3 个请求，剩余 30 条等待空闲请求继续执行
```

缓存命中的文本不会进入 API 请求。

## 十、加载图标提示

当某段文本需要调用 DeepSeek API，且尚未返回译文时，插件会在原文后显示一个旋转加载图标。

缓存命中的文本会直接替换，不显示加载图标。

当 DeepSeek API 返回译文后：

- 加载图标会自动移除
- 原文会被替换为译文
- 译文会写入 IndexedDB 本地缓存

## 十一、恢复原文

点击「恢复原文」后，插件会尝试将当前页面中已替换的文本恢复为原始文本。

恢复原文只影响当前页面，不会删除本地缓存，也不会关闭当前域名的全站翻译配置。

## 十二、清除缓存

点击「清除缓存」会清除当前域名的 IndexedDB 翻译缓存。

它不会：

- 删除 API Key
- 关闭全站翻译
- 删除网站配置
- 清除其他网站缓存

它会导致当前网站之后遇到相同原文时重新调用 DeepSeek API 翻译。

## 十三、管理已开启的网站

插件弹窗底部有「已开启的网站」列表，可以查看所有开启了全站翻译的域名。

每个网站有两个操作：

- 「关闭」：关闭该网站全站翻译，但保留目标语言配置。
- 「删除」：删除该网站配置，并清除该域名的 IndexedDB 翻译缓存。

## 十四、当前翻译提示词

插件调用 DeepSeek API 时使用固定系统提示词：

```text
You are a translation engine. Translate every item to {targetLang}. Preserve meaning, tone, numbers, punctuation, URLs, placeholders, and HTML entities. Return only valid JSON in this shape: {"items":[{"id":"same id","translation":"translated text"}]} Do not add explanations.
```

其中 `{targetLang}` 会替换为插件中选择的目标语言，例如 `zh-CN`。

请求参数包含：

```json
{
  "response_format": { "type": "json_object" },
  "temperature": 0,
  "thinking": { "type": "disabled" }
}
```

## 十五、常见问题

### 1. 为什么有些页面不能翻译？

Chrome 扩展无法注入部分特殊页面，例如：

- `chrome://` 页面
- Chrome Web Store 页面
- 某些受浏览器保护的页面

这些页面属于浏览器限制。

### 2. 为什么输入框、代码块没有翻译？

插件会主动跳过输入框、代码块、可编辑区域和脚本样式区域，避免破坏网页功能。

### 3. 为什么翻译后页面布局有变化？

译文长度可能和原文不同，部分网页布局可能被撑开。这是页面翻译类插件的常见现象。

### 4. 为什么换模型后仍然看到旧译文？

因为本地缓存命中后不会再次调用 API。可以点击「清除缓存」后重新翻译当前域名。

### 5. 是否可以把插件 zip 直接加载？

不能。Chrome 开发者模式只能加载已解压的扩展文件夹。zip 适合备份、传输或上传 Chrome Web Store。

### 6. 多个人使用同一个 zip 包时，API Key 会共享吗？

不会。zip 包里只有插件代码和图标资源，不包含使用者填写的 DeepSeek API Key。每个人在自己的 Chrome 里加载插件后，需要在插件弹窗中填写自己的 DeepSeek API Key。

### 7. 解压目录必须和说明里的路径一致吗？

不需要。目录地址就是每个人电脑上的实际解压地址。加载扩展时，只要选择包含 `manifest.json` 的解压后文件夹即可。

## 十六、安全说明

插件 zip 包可以共享给其他使用者，但 DeepSeek API Key 需要每位使用者自行配置。API Key 存储在各自本机 Chrome 扩展配置中，不会写入 zip 包或插件目录。

请注意：

- 不要在截图、录屏或文档中暴露自己的 API Key。
- 不要把已填写 API Key 的浏览器配置文件分享给别人。
- 如果修改插件代码后重新打包 zip，请确认没有把个人密钥写入任何源码文件。

